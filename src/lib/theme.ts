import { IColors } from "@/types/theme";

const white = '#fff'
const black = '#000'
const grey = '#ccc'
const lightGrey = '#eee'
const darkGrey = '#939799';

export const colors: IColors = {
  white,
  black,
  grey,
  lightGrey,
  darkGrey,
}